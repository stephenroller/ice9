# nested function calls with local vars & temps (tests popping of AR)

proc bar (i, j : int) : int
	var l, m : int
	
	l := i + j;
	m := 2 * l;
	bar := l + m;
end

proc foo (i, j : int) : int
	var l, m : int
	
	l := 2 * i;
	m := 2 * j;
	
	foo := bar(l, m);
end

write foo(1, 1);

