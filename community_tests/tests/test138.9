# second example from spec clarification					succeed

proc f()
	type int = bool
	
	var b : int			# uses level 2, proc
	b:= true;
	
	write ?b;
end
	
var a : int				# uses level 0, built-in
	
a:= 1;
f();

write a;