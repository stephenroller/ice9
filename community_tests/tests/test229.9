# proc list test											succeed

proc foo(a, b, c, d, e : int, f, g, h, i : bool)

	a := 0;
	b := 0;
	c := 0;
	d := 0;
	e := 0;

	f := true;
	g := true;
	h := true;
	i := true;
end

foo(0, 1, 2, 3, 4, true, true, true, true);
