# 2D array in proc as param to another proc

type array2 = int[1][2]

proc bar(pi2 : array2)
	write pi2[0][0];
	write pi2[0][1];
end

proc foo()
	var pi : array2
	
	pi[0][0] := 13;
	pi[0][1] := 16;
	bar(pi);
end

foo();