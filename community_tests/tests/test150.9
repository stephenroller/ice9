# type checking example from forum						succeed
# this also tests the different symbol tables (e.g., proc foo (foo : foo))

type foo = int[2][2][2]
type bar = int[2][2]
type baz = bar[2]

var baz : baz

proc foo (foo : foo)
	write foo[1][1][1];
end

baz[1][1][1] := 4;
foo(baz);
